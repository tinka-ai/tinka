import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({
    OPENAI_API_KEY: process.env.OPENAI_API_KEY ? "LOADED" : "MISSING",
    TINKA_ASSISTANT_ID: process.env.TINKA_ASSISTANT_ID ?? "missing"
  });
}
